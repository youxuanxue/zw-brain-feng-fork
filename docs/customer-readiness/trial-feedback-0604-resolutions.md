# 客户试用第一轮反馈（0604）逐条解决对照表

> 来源：`old/问题反馈/问题反馈-0604.xlsx`（16 条，业务方两位试用人员）。
> 修复载体：PR #213（四组并行集成：用户语言层+R12 渲染层守卫 / 流程收口+404 / 分型详情+筛选 / 数据呈现+角色投影）。
> 状态口径：**已解决**（本 PR 内可验证）/ **拒绝**（明确不做+理由）/ **未解决**（记账延后）。
> 验证方式：干净重建真实库 + Playwright 真 UI 走查 + 全量 `--with-e2e` 测量（0 fail）。

| # | 模块 | 反馈摘要 | 状态 | 解决方式 / 证据 | 遗留（已记账） |
|---|---|---|---|---|---|
| 1 | 各模块 | 按钮不明显，像描述文字 | **已解决** | 顶部 9-tab 词墙重构为旅程分组左侧侧边栏；页头快捷链接改胶囊按钮；主操作实心/次操作描边分级 | — |
| 2 | 各模块 | 页面无模块介绍 | **已解决** | 侧边栏每项配一行业务描述（如「找数据——搜索可复用的政务数据资源」）；每页页头一句话定位 | — |
| 3 | 发起复用申请 | 「复用申请」叫法歧义，建议改名 | **已解决** | 产品负责人拍板定名「申请资源」，按钮/提示/引导语全链改名 | — |
| 4 | 发起复用申请 | 「可复用」标签每个资源都有，可去掉 | **已解决** | 默认发现视图已按可用性过滤，同义反复标签抑制；非默认态（待发布等）保留 | — |
| 5 | 发起复用申请 | 目录详情信息不能少于旧平台（编制规范） | **已解决** | 编制规范字段全集进详情：首屏决策字段（共享类型/条件/更新周期）+ 摘要 + 折叠编目字段；码值解码为业务中文（`tests/e2e/typed_resource_detail.spec.ts`） | 部分字段上游导入缺供→诚实空态+补导入记账（接口服务化绑定、内部部门等） |
| 6 | 发起复用申请 | 四种资源类型详情无区分、信息太少 | **已解决** | 按资源类型分型块（库表/文件/文件夹/接口/链接）+ 类型徽标；字段数据模型 404 根因修复（部署前缀对齐），有数据真展示、无数据诚实空态 | 接口资源本批导入无服务化绑定→模板就位+诚实空态，补导入另立项 |
| 7 | 发起复用申请 | 筛选项太少（至少名称/类型/部门） | **已解决** | 名称检索 + 资源类型筛选 + 提供部门筛选（真实库现算，与智能检索共存） | — |
| 8 | 发起复用申请 | 起草后找不到草稿、无法确认提交 | **已解决** | 根因=REST 成功信封取号缺失；修复后起草直达草稿详情页（确认→提交闭环，`draft_request_closure.spec.ts`） | — |
| 9 | 平台指南 | 应答业务问题、按角色提示，部署问题不应在内 | **已解决** | 快捷问题业务化（目录如何注册/如何申请等），工程示例问题（Docker/REST/端口）移除 | 按角色切片语料（不同岗位不同快捷问题）作为增强已列计划 |
| 10 | 工作台-办理建议 | 描述太技术（能力包等概念看不懂） | **已解决** | 办理建议改为业务句式从真实积压现算生成；R12 渲染层守卫（13 场景页禁工程词形，`r12_rendered_language.spec.ts`）防回潮 | — |
| 11 | 工作台-今日待办 | 待办数据不准、点击无处理页面 | **已解决** | 待办从真实库积压现算（待发布/待审核目录、待审核资源、待受理申请、待补全用途），每条深链直达办理页，零积压不渲染（`workbench_todo_closure.spec.ts`） | 角色待办语义定义随 sign-off 包待业务方签字确认（feature 保持 InTest） |
| 12 | 可复用资源 | 检索结果出现 discover_resource 等技术词 | **已解决** | 意图回显走术语映射；「自然语言加速器」整体更名「智能检索」（采纳试用人员建议词） | — |
| 13 | 专题包 | 是什么场景、谁维护、几种分类看不懂 | **已解决** | 占位描述（projection 等）源头改写+渲染层降级双保险；侧边栏与页头补一句话说明（按场景订阅成套共享数据） | 专题包策展治理（运营策展 vs 自动目录包分区、分类体系）已列后续计划 |
| 14 | 办共享申请 | 我的已办/我的申请/我的授权混在一起 | **已解决** | 拆四视图：我的申请/待我办理/我的授权/国家通道；无权岗位对应视图整体不渲染（`role_projection_views.spec.ts`）；脏用途降级「未填写用途」、历史导入/在产来源标识 | 视图定义随 sign-off 包待业务方签字确认 |
| 15 | 交付回执 | 定位不清（凭证/对账/与交换如何对接） | **已解决** | 页首一句话定位（领凭据、对账回执在此办理）；渠道/时间/编号渲染规范化（交换通道等业务词，无裸 token/时间戳） | 与交换平台（一表通）业务对接深化属跨系统集成主线，另行推进 |
| 16 | 维护数据供给 | 「字段裁决」新概念看不懂 | **已解决** | 更名「字段审核」（仅用户语言层，路由/能力标识/权限门不变） | — |

合计：**已解决 16 / 拒绝 0 / 未解决 0**（5 条带已记账遗留增强，均不阻塞本轮验收）。

## 附：试用现场补充反馈（0604 负责人真 UI 走查，同 PR 内闭环）

| 反馈 | 状态 | 解决方式 |
|---|---|---|
| 工作台加载时间长 | **已解决** | 根因=认证就绪竞态（挂载早退无补触发）；修 watch 就绪即刷 + 按岗位缓存秒显 |
| 边框/布局太紧 | **已解决** | 全页面 8pt 间距节奏三轮收齐（容器/卡片/表格/网格统一） |
| 接入管理文字太技术 | **已解决** | 能力接入/开放范围/流程与表单配置改名 + 卡片文案人话化 | 
| 按钮改「申请资源」+ 快捷入口形态 | **已解决** | 改名全链落地；页头快捷链接重设计为胶囊按钮 |
| 导航栏窄一点、标题大一点 | **已解决** | 侧边栏收窄、页面标题字号提级（详见 PR 记录） |

> 第二轮走查建议：两位试用人员按本表逐行复核，验收标准——全程不需要问「这是什么意思」。
