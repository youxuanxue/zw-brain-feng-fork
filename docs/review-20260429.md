# 代码审查报告

> 审查日期：2026-04-29
> 审查范围：全仓 tracked files（忽略 gitignored）；最近 24 小时 commit：c139527, 164d624, e7dbf0a
> 风险等级：high
> 审查模式：full_conformance
> 审批产物：已加载 docs/approved/*.md（8 份，frontmatter 均为 approved）

## 摘要
- 结论：merge-ready
- CRITICAL/HIGH: 0 项
- Validation gaps: 段 12 fixture-coverage 仍按 `docs/preflight-debt.md` 登记为 GATE-2 后激活事项；浏览器端 WebUI E2E 待最终人工验收补充

## 已修复发现

### [R-001] [zw_brain/shared/runtime.py:1] shared 层反向依赖 command/domain
- 状态：Resolved + Verified
- 修复：应用装配迁入 `zw_brain/command/runtime.py`，入口层统一从 command runtime 获取 service；`shared/runtime.py` 不再导入 command/domain。
- 验证：`PYTHONPATH=. .venv/bin/python -m pytest tests/test_entry_runtimes.py ...` 已覆盖入口运行时。

### [R-002] [zw_brain/entry/mcp/server.py:20] Capability exposure 未成为运行时边界
- 状态：Resolved + Verified
- 修复：`zw_brain/skill_registration/runtime.py` 增加 surface helper；`scripts/export_agent_contract.py` 按 compatibility 过滤 REST/MCP/A2A 投影；REST/CLI/MCP/A2A 入口调用前执行 surface 校验。
- 验证：MCP 生成物不再包含 `request.create`；`tests/test_contract_projection.py` 与 `tests/test_entry_runtimes.py` 已覆盖正负向边界。

### [R-003] [zw_brain/shared/audit/__init__.py:38] 审计总线未自身同步 durable 落库
- 状态：Resolved + Verified
- 修复：audit bus 现在要求 durable sink，同步写入失败抛 `AuditWriteError` 并阻断写路径；`BrainService` 不再直接 append audit DB。
- 验证：`tests/test_brain_service.py::test_audit_sink_failure_blocks_write_mutation` 覆盖失败熔断。

### [R-004] [zw_brain/command/brain.py:27] BrainService 膨胀为权限/状态机/投影混合层
- 状态：Resolved + Verified
- 修复：权限矩阵、角色解析、tenant/permission policy 下沉到 `zw_brain/domain/policy.py`；`BrainService` 只保留 command 层异常转换与编排。
- 验证：`tests/test_domain_policy.py` 覆盖权限策略；`tests/test_brain_service.py` 保留主流程回归。

### [R-005] [docs/approved/zw-brain-architecture-v4-gpt55.md:910] 设计基线与实际 preflight 门禁漂移
- 状态：Resolved
- 修复：approved 附录 C 已将段 13 改为历史 D21/已退役；`docs/preflight-debt.md` 增加防漂移说明。
- 验证：后续由 `bash scripts/preflight.sh` 的 approved/stat/external-ref 门禁覆盖。

## 符合性结论

- 分层边界：已恢复为 entry → command → domain → shared；shared 不再承担应用装配。
- 统一能力契约：compatibility 成为投影生成和运行时调用的共同边界。
- 审计边界：audit bus 变为同步 durable sink 门面，写失败阻断 mutation。
- OPC 机械守卫：段 13 当前态已从 approved 基线移除，避免文档与门禁漂移。
