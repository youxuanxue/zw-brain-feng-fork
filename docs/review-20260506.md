# 代码审查报告

> 审查日期：2026-05-06
> 审查范围：f67e9a8（feature/true-data-demo-seed: A1–A4 真数据 demo seed）
> 风险等级：normal
> 审查模式：concise
> 审批产物：未加载（本次未触达 docs/approved/）

## 摘要

- 结论：**needs-fix**
- CRITICAL/HIGH：2 项
- MEDIUM：4 项
- LOW：2 项
- Validation gaps：discovery.zones 一致性与 recallDictionary 内容质量未在落地校验中覆盖

## 关键发现

### [R-001] zw_brain/command/brain.py:1852 — 关键词旁路使搜索退化为「全部返回」

- 严重程度：high
- 问题：`if not haystack or haystack in text or any(token in query for token in [...])` 是 OR 子句。query 含「停车场/城市/交通/法人/企业/模板/复用」任一 token 时，所有 12 张 discovery 卡都被命中。本次新增的三个 token 在 A1 真目录卡落地后变成纯负担，原有 4 个 token 也是同类反模式遗留。
- 修复：移除整段 OR 旁路，回到 `if not haystack or haystack in text:`。A1 卡片的 name/desc/fields/explain 已包含这些关键词，自然命中即可。

### [R-002] scripts/build_true_data_seed.py:171 — 停车场卡引用未声明的 zone「城市运行专区」

- 严重程度：high
- 问题：两张停车场卡 zone 设为「城市运行专区」，但顶层 `zones` 与 `discovery.zones` 只声明了 营商环境/治理减负/民生保障。zone 筛选器与 zone 详情页将无法解析这两张卡。
- 修复：把停车场卡 zone 改回「营商环境专区」（OPC 极简，不为 2 张卡多开一个 zone）。

## 必修问题（medium）

### [R-003] scripts/build_true_data_seed.py:414 — recallDictionary 80% 是 retired 目录

- 25 个 sample_titles 中有 20 条 lifecycle_status=retired。NL skill 召回字典本来要给真候选，结果绝大多数是废弃目录。
- 修复：SQL WHERE 加 `lifecycle_status IN ('active','approved_pending_publish')`。

### [R-004] scripts/build_true_data_seed.py:456 — REAL_ORGS 死代码

- 定义后没有调用方。Jobs「最小 API 面」反模式。
- 修复：直接删除整段常量。

### [R-005] scripts/build_true_data_seed.py:559 — workbench 文案与 catalogTree 数字打架

- workbench r1.subtitle 写「18,752 条 pub_organ projection」，但同一脚本 catalogTree 动态查询得到 18,750。同一刷新里两条出口数字不一致。
- 修复：改成近似口径「约 1.8 万条」，避免快照值漂移。

### [R-006] scripts/build_true_data_seed.py:66 — CASE_OVERRIDES 用脆弱的 hex package_code 作 key

- 32 字符 hex ID 漂移时所有手工策划文案静默退化为通用模板，无告警。
- 修复：改用稳定的 `pkg['title']` 作 key。

## 可选建议（low）

### [R-007] docs/reconstructs/legacy-import-mapping-v1.md:522 — §5.5 快照数字未标日期

- 附录 A 已立规：行级快照数应标「快照 @YYYY-MM-DD」。
- 修复：在 §5.5 catalogTree 这行尾追加「（dump 快照 @2026-05-06）」。

### [R-008] scripts/build_true_data_seed.py:289 — `_resource_id_from_title` 生成的 id 含中文

- `res-tp-小微企业一次性创业岗` 与 ASCII 约定 (`res-jbxx-ledger` 等) 不一致。URL 路由可工作但语义不干净。
- 本次不阻塞合入；下个迭代统一为 ASCII 短码。

## OPC / 乔布斯哲学违反点合计

- **R-001/R-004**：留着不用的旁路与常量 → 违反 Jobs「最小 API 面」与 OPC「流程极简」
- **R-002/R-005**：自己制造的不一致 → 违反 OPC「反脆弱」
- **R-003**：默默给 NL skill 喂垃圾候选 → 违反 Jobs「精品意识」
- **R-006**：脆弱 key 让策划文案说没就没 → 违反 OPC「反脆弱」+「自动化优先」
