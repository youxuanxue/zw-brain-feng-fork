---
description: dev-rules submodule 约定：规则的单一事实来源管理规范
alwaysApply: true
---

# dev-rules 约定

## 项目结构

每个 IT 产品研发项目必须包含 `dev-rules` 作为 git submodule。仓库入口索引见 `dev-rules/README.md`；接入、同步与门禁细节以本文件为准。

```
项目根目录/
├── dev-rules/                       ← git submodule
├── .cursor/rules/*.mdc              ← sync 产物（禁止直接编辑）
├── scripts/preflight.sh             ← 可选项目 wrapper
└── .git/hooks/pre-commit            ← install-hooks.sh 安装；按
                                        scripts/preflight.sh
                                        → dev-rules/templates/preflight.sh fallback

家目录消费端（symlink，由 sync.sh 维护）：
~/.cursor/rules/      → dev-rules/rules/
~/.claude/commands/   → dev-rules/commands/
~/.claude/CLAUDE.md   → dev-rules/global/CLAUDE.md
```

## 新项目接入

```bash
cd 项目根目录
DEV_RULES_REMOTE_URL="${DEV_RULES_REMOTE_URL:?set DEV_RULES_REMOTE_URL}"
git submodule add "$DEV_RULES_REMOTE_URL" dev-rules
dev-rules/sync.sh --local                   # 复制规则 + 自动 register 该项目
git add .cursor/rules/ .gitmodules dev-rules
git commit -m "chore: add dev-rules submodule and sync rules"
```

**首次在本机使用**（仅一次）：

```bash
DEV_RULES_REMOTE_URL="${DEV_RULES_REMOTE_URL:?set DEV_RULES_REMOTE_URL}"
git clone "$DEV_RULES_REMOTE_URL" ~/Codes/dev-rules
~/Codes/dev-rules/sync.sh                                          # 创建 home symlinks
bash ~/Codes/dev-rules/templates/install-launchagent.sh            # 注册跨机器同步 agent
```

之后在任意项目里 `sync.sh --local` 就会自动把项目 `(name, git remote URL)` 写进 `~/Codes/dev-rules/.registered-projects`（git-tracked，跨机器共享），并把 `(git URL, 本机绝对路径)` 写进 `.local-projects`（per-machine，gitignored）。下一次 `--push` / `--pull` / LaunchAgent 会按本机实际落地的项目自动 fan-out。

fan-out 使用 `.registered-projects` 与 `.local-projects` 的并集：跨机器登记项目在本机有路径会同步；只存在于 `.local-projects` 的本机项目也会同步。后者适合 zw-brain 这类不希望在项目仓库暴露 dev-rules 远端 URL、但仍希望从 `~/Codes/dev-rules` 获得规则副本的项目。

换机器时，跨机器项目重新 `--local` 一次即可重建本机映射；本机-only 项目需要在该机器写入 `.local-projects` 或手动执行 `sync.sh --project /path/to/project`。

## 规则载体分工（防复杂度膨胀）

<!-- rule-carrier-partition-anchor -->

系统级审视结论只落到既有载体，不新增流程型散文档：

- `global/CLAUDE.md`：会话级硬纪律，只写 Agent 每次都要执行的短规则。
- `rules/product-dev.mdc`：研发路径、风险分级、PR / commit 形状与完成自检。
- `rules/agent-contract-enforcement.mdc`：WebUI / API / CLI / MCP 契约、安全基线与跨端对齐。
- `rules/test-philosophy.mdc`：Story 与测试强度的升级条件。
- `commands/*.md`：命令执行步骤与输出契约，不承载新的长期规则体系。
- `README.md`：短入口索引；不展开接入细节、哲学、风险分级或测试方法。
- `digital-clone-research.md §二`：软规则到硬检查的映射表；每行必须对应会失败的脚本或 schema。
- `templates/` 与 `scripts/`：硬约束实现；反复出现的问题优先在这里落地。
- `.registered-projects` / `.local-projects`：只表达项目落地状态；本机观察只有转成 sync / preflight / verify 检查或项目 wrapper debt 后，才进入长期规则。

同一约束只在一个主载体展开；其它载体最多保留一句索引或检查入口。若暂时无法机械化，放进 review finding 或项目 `docs/preflight-debt.md`，不要新增“靠阅读自觉”的长期流程文档。

## skill / command 确定性基线（机械化优于 prompt 推断）

这是 OPC「自动化优先」落到 skill / command 作者身上的元规则，**适用于本仓库的 `commands/*.md`，也适用于任何项目自建的 `.cursor/skills/**/SKILL.md`**。同一段 prompt 让模型每次重新推断同一件可计算的事，就是不确定性来源；能机械化的必须由脚本/工具承载，prompt 只保留真实判断。

**必须脚本化（确定性 > 模型推断）**：计数与字数、结构化文件解析（JSON / YAML / frontmatter）、查表与映射、按字段排序 / 去重 / 配额筛选、抓取与重试、格式 / schema 校验、路径安全校验、状态/活性派生。判据：输入相同则输出必相同、且无需创意——这类步骤写成 prose 让模型执行属于反模式。

**应保留为 prompt（真实判断，过度机械化是另一种反模式）**：选题/角度、文案与标题创意、视觉与编辑审查、架构权衡、风险等级里的「爆炸半径 / 回滚成本」判断。

作者基线：

- 新增或修改 skill / command 时，先列出其步骤里哪些是「可机械化」、哪些是「真判断」；可机械化的实现成脚本/工具并由 skill 调用，模型只消费其确定性输出（典型：`/twin` 把状态推进全塞进 `python3 -m scripts.twin` 子命令，模型逐字转发 stdout）。
- 复用既有确定性工具，不重造：契约 / 分层 / Web surface / 高风险锚点 / 存在性测试 / approved frontmatter / 静默吞错等已由 `dev-rules/scripts/check_*.py` + `templates/preflight.sh` 覆盖；skill / command 应**调用** preflight 取 ground-truth，而非让模型重新肉眼判断（参见 `commands/xj-review.md` §0 机械门禁先行）。
- 反复出现的机械化缺口按 `digital-clone-research.md §二` 落成会失败的脚本 / schema；纯创意步骤不强行工具化。

**review 时的保障**：`/xj-review` 的 OPC 准则已把「本可机械化检查却写成 prose 规则」列为必报 finding——这就是本基线的 review-time 门禁，新增 skill / command 时按此自审，不另设易误报的新 check。

## 编辑与提交规则

涉及 `dev-rules/` 的变更**必须**先在子模块完成 commit + push，再在父仓库提交子模块指针与 `.cursor/rules/`。颠倒顺序会让父仓库指向一个远端不存在的 SHA，协作者与 CI 都会失败。

### 标准链路

```bash
# 1. 在任意项目内编辑 dev-rules/{rules,commands,global}/* 后：
dev-rules/sync.sh --local          # 同步到本项目 .cursor/rules/ + 自动 register
cd dev-rules
git add -A && git commit -m "update rules"
./sync.sh --push                   # = git push + ~/Codes mirror pull + 所有项目 fan-out
cd ..
git add dev-rules .cursor/rules/
git commit -m "chore: sync dev-rules" && git push
```

`sync.sh --push` 是推荐的一步入口；断网或 debug 时可拆为 `git push` + `cd ~/Codes/dev-rules && git pull --ff-only && ./sync.sh --all`，效果完全等价。

### 跨机器同步（被动）

LaunchAgent `local.dev-rules.sync` 每 30 分钟跑 `sync.sh --pull`，把其他机器推上来的更新拉到 `~/Codes/dev-rules` 并 fan-out 到本机已落地的注册项目。如果 `verify-rules.sh` 报告 LaunchAgent 未装，跑 `templates/install-launchagent.sh`。

## 强约束门禁（机械检查，禁止仅"靠自觉"）

每条软规则都有对应的可执行脚本。**完整的「软规则 → 硬检查」映射表是单一事实来源**，定义在 `dev-rules/digital-clone-research.md §二`。本节只列接入步骤：

1. **项目级 wrapper（可选）**：仅当项目有「dev-rules 模板未覆盖的特异检查」时，`cp dev-rules/templates/preflight.sh scripts/preflight.sh` 并在 wrapper 里追加项目段。否则**不需要**新建 `scripts/preflight.sh`——`install-hooks.sh` 会自动 fallback 到 `dev-rules/templates/preflight.sh`。
2. **安装 git hook**：`bash dev-rules/templates/install-hooks.sh`。Hook 在运行时按以下顺序解析 preflight：
   ```
   $REPO_ROOT/scripts/preflight.sh           ← 项目 wrapper（若存在）
   $REPO_ROOT/dev-rules/templates/preflight.sh   ← 模板（fallback）
   ```
   增删项目 wrapper 后**无需**重装 hook。
3. **CI 同步运行**：在 GitHub Actions / GitLab CI 中追加一步 `./scripts/preflight.sh`（若存在）或 `./dev-rules/templates/preflight.sh`。
4. **dev-rules 子模块自身**：源仓库内置 `scripts/preflight.sh`（调用 `./verify-rules.sh` + `./sync-stats.sh --check`），首次克隆后跑一次 `bash templates/install-hooks.sh` 即可让 pre-commit 自动拦截违例提交。**不要靠记忆手动跑 verify-rules.sh**——这条规则正是 OPC「自动化优先」要消灭的"自觉履约"。

接入完成后，违反任何一条强约束的提交都会被 hook 拦截，违反者的 CI 会 fail。

如果某项检查段在该项目尚不可自动化（例如还没有 API 时跳过 contract drift），必须把缺口记入 `docs/preflight-debt.md` 并设定截止日期，**禁止悄悄降级为"靠自觉"**。

## approved-doc status 词汇（跨项目共享）

`docs/approved/*.md` 的 frontmatter `status` 字段使用统一词汇表，由 `dev-rules/scripts/check_approved_docs.py` 强制（preflight § 7 R1-R4，任何分支）：

| status     | 语义                                                                            |
| ---------- | ----------------------------------------------------------------------------- |
| `draft`    | 早期草稿，可自由修改                                                                    |
| `pending`  | 设计已提出，等待人工审批                                                                  |
| `approved` | 已审批基线，变更需走 GATE / PR review（zw-brain GATE 模型主用）                              |
| `shipped`  | 实现已合并，状态从 pending/approved 翻转而来                                               |
| `archived` | 被取代或不再权威                                                                      |

附加不变量：

- **R3** `pending + (related_prs OR related_commits)` → 拒绝（"shipped under pending" 烟雾）
- **R4** `shipped` 必须列出 `related_prs` 或 `related_commits`（审计链不许断）
- **R5** `approved_by: pending` 不许 land 到 `main` / `master`（preflight § 7 R5，仅在 main/master 触发；其它分支可临时持有 pending approver）

项目特异的更细分类（如 zw-brain 的 `gate: GATE-1..GATE-5`）属于 wrapper 层职责，不进入此通用词汇表。

## 禁止事项

- 禁止直接编辑 `.cursor/rules/` 中的文件（它们是 sync 产物）
- 禁止把 `~/Codes/dev-rules/` 用作"另一个上游"——每个项目的真相是它自己的 submodule SHA
- 禁止在项目里把 `.cursor/rules/*.mdc` 改为 symlink 指向 `~/Codes/dev-rules/`（云端 Agent 拿不到目标文件 + 上游错乱）
- 禁止在 `dev-rules/sync.sh --check` 失败的情况下提交父仓库
- 禁止在 `dev-rules/verify-rules.sh` 失败的情况下提交 dev-rules 子模块
- 禁止在 macOS dev 机器上把 LaunchAgent 卸载后不重新装回——跨机器同步路径会变回"承诺"

## Agent 行为约束

- 项目缺少 `dev-rules/` submodule：提醒用户按「新项目接入」接入
- `.cursor/rules/` 与 `dev-rules/rules/` 内容不一致：跑 `dev-rules/sync.sh --local` 修复，再用 `--check` 确认归零
- `dev-rules/sync.sh --status` 报告 mirror SHA 与 submodule SHA 不一致：先 `--push`（本机有未推变更）或 `--pull`（远端有未拉变更），不在落后状态下做新规则编辑
