---
description: IT 产品研发工作流规则，默认单 PR，按风险升级到高风险审批路径
alwaysApply: true
---

# 产品研发工作流

## 设计哲学（贯穿全流程）

**产品设计遵循乔布斯理念，研发与运维遵循 OPC（One-Person Company）哲学。**

- **聚焦**：对一千件事说不。每个功能必须先回答「它应不应该存在」，再回答「怎么实现」。
- **简洁**：简洁是复杂的最终形态。API、代码、UI 都应追求「看起来显然没有多余的东西」。
- **端到端**：用户感知的是完整旅程。不是把各模块做好再拼凑，而是从第一次接触到最终完成任务的一致体验。
- **精品意识**：宁可少做，做出来的每一个功能都必须达到「可以自豪地展示」的水准。
- **杠杆最大化**：人只做需要判断力的事（审批门禁 + 架构决策），其余一切交给 Agent 自动化。
- **流程极简**：每个流程步骤必须挣得自己的位置。不能证明价值的步骤直接去掉。
- **自动化优先**：能写成脚本/CI 检查/Rule 的，绝不依赖 Agent 每次自觉遵守。完成自检（见下方）就是把"靠记忆"转化为"靠规则"。

## 默认工作流

默认路径不是把 docs、prototype、开发、上线拆成多个 PR，而是：

```text
单 PR → 实现/测试 → preflight → review → 人工确认
```

- 默认一个 PR 只承载一个用户意图，不按阶段机械拆 PR
- 默认直接做生产级实现，不先做“为了走流程而存在”的原型 PR
- 默认用最少的文档承载必要判断，不把 PR、comment、文档写成四份重复说明

只有命中**高风险变更**时，才升级到原型 + 审批路径。

## 风险分级（决定走哪条路径）

### 低风险

符合以下特征之一，且不触发下方高风险条件：

- 文档、注释、命名、格式化
- 无公共行为变化的内部重构
- 无公共契约变化的依赖升级
- 范围清晰的小型 bug 修复，且可用回归测试验证

**默认路径**：单 PR，直接实现与验证。

### 常规风险

会改变功能行为，但不触发高风险条件的日常开发：

- 新增或修改局部功能
- 影响单一模块或单一用户旅程的实现调整
- 需要补充测试、契约同步或少量设计说明，但无需人工设计审批

**默认路径**：单 PR，必要时补充任务拆解、测试与契约文档。

### 高风险

只有命中以下条件，才进入高风险路径：

- **公共契约变更**：API / CLI / MCP 的对外行为、字段语义、状态码、兼容性
- **不可逆状态变更**：数据库 schema、状态机迁移、审批链/计费链等高代价状态
- **安全边界变更**：鉴权、授权、输入安全、租户隔离、凭证/密钥路径
- **高回滚成本骨架变更**：核心架构边界、基础设施策略、跨系统集成，且一旦出错不易快速回滚
- **核心体验高爆炸半径变更**：用户核心路径一旦出错会产生明显业务损失，且普通测试/PR review 不足以兜底

以下信号**不能单独**判定为高风险：

- 改动文件多
- 代码量大
- 涉及多个模块
- PR 评论看起来很长

如果对风险等级拿不准，默认按**常规风险**处理，并先补一个最小澄清问题；只有明确命中高风险条件，或存在安全/数据损失级不确定性时，才升级为高风险。

## 高风险路径（例外，而非默认）

```text
需求分析 → 原型设计 → [人工审批] → 功能实现 → 测试验证 → [人工审批] → 合并上线
```

只有高风险变更必须按顺序通过；低风险与常规风险不应被此路径拖重。

### 阶段 1：需求分析

**产出**：风险明确的拆解文档（`docs/task-breakdown-*.md`）
**动作**：
1. 理解业务目标和用户价值
2. **聚焦过滤**：明确列出「不做什么」和「为什么不做」，与「做什么」同等重要
3. 判断是否真的属于高风险；如果不是，降回默认路径
4. 拆解为可执行子任务（使用 `/user:decompose` 命令）
5. 标注子任务间的依赖与并行关系
**审批**：仅当存在真实设计决策时，才需要人工确认后进入下一阶段

### 阶段 2：原型设计与实现

**产出**：可运行的原型系统 + 设计文档（直接写入 `docs/approved/`）
**动作**：
1. 将设计文档直接写入 `docs/approved/`（实体化命名，如 `design-approval-engine.md`），包含：
   - YAML frontmatter 元数据头（`approved_by: pending`，待人审批时更新）
   - 数据模型设计（ER 图或表结构）
   - API 接口设计（路由、请求/响应格式）
   - 核心流程的状态机或时序图
   - 技术选型与理由
2. 实现最小可运行原型（Prototype），仅覆盖核心路径
3. 原型必须可启动、可演示核心流程，但无需完善边界处理
4. 编写原型验证脚本或手动验证步骤
**提交**：只有当“设计确认”与“实现落地”存在真实决策边界时，才单独创建 `prototype:` PR；否则可在同一 PR 中以明确 commit / section 区分原型与落地

### ▶ 审批门禁：原型确认

**Agent 在此暂停，等待人工审批。**

审批内容：
- [ ] 数据模型是否合理
- [ ] API 设计是否符合现有规范
- [ ] 技术选型是否恰当
- [ ] 原型是否可演示核心流程
- [ ] 是否需要调整设计方向

审批方式：人在 PR 中直接编辑/修正 `docs/approved/` 下的设计文档，merge = 审批。

审批结果：
- **通过（merge）** → `docs/approved/` 中的文件成为符合性基线，进入功能实现阶段
- **需修改** → 人在 PR 中提出修改意见，Agent 修改后重新请求 Review
- **否决** → 终止当前方案，回到需求分析重新规划

### 阶段 3：功能实现

**产出**：生产级代码 + 测试 + 必要文档
**动作**：
1. 基于已审批的原型，补全所有边界处理和错误处理
2. 编写完整的测试套件（遵循 test-philosophy.mdc）
3. 更新 API 契约文档（遵循 agent-contract-enforcement.mdc）
4. 将实现收敛到一个用户意图明确的 PR 中；只在确有必要时拆分
**提交**：PR 标题聚焦结果，不写冗长过程清单

### 阶段 4：测试验证

**产出**：验证摘要 + 验收确认（默认精简；高风险时再附扩展材料）
**动作**：
1. 运行完整测试套件，确保不破坏已有功能
2. 验证所有验收标准（AC）已覆盖
3. 检查 Story ↔ Test 对齐（无漂移）
4. 生成变更摘要：改了什么、为什么改、如何验证
**提交**：默认在同一 PR 中附带精简验证摘要；高风险变更才附完整验收矩阵与额外证据

### ▶ 审批门禁：合并确认

**Agent 在此暂停，等待人工审批。**

审批内容：
- [ ] 代码质量是否达标
- [ ] 测试覆盖是否充分
- [ ] 是否影响其他模块
- [ ] 是否可以合并到主分支

## 分支与提交

- 原型分支命名：`prototype/[issue-id]-[描述]`（仅高风险原型路径使用）
- 功能分支命名：`feature/[issue-id]-[描述]`（默认）
- 修复分支命名：`fix/[issue-id]-[描述]`
- 杂务分支命名：`chore/[描述]`
- 文档分支命名：`docs/[描述]`
- 上游合并分支命名：`merge/upstream-YYYY-MM-DD`（消费 fork 的项目使用，受 PR shape 工作流验证）
- 提交消息聚焦"为什么"而非"改了什么"，一行说清单个原子变化的原因
- 每个 PR 只解决一个问题，不混合无关变更
- 不默认拆出 docs/prototype/dev/release 多个 PR；只有真实决策边界或风险隔离需求，才拆 PR
- PR 描述默认只保留 `Summary`、`Risk`、`Validation`；只有高风险变更才展开 `Design` / `Contract` / `Migration`
- review comment 默认只报阻塞问题、真实风险、缺失验证；不输出大而全的审计散文

## 代码质量门禁

- 新增功能：至少 1 个正向测试 + 1 个负向测试
- Bug 修复：必须包含复现测试
- 重构：已有测试必须原样通过
- 禁止存在性测试（仅断言文件存在/非空），必须是行为断言

## 长时运行任务专用

当作为 Background Agent 或 Long-running Agent 执行时：
- 每完成一个可验证的里程碑，立即提交并推送
- 如果连续 3 次尝试同一问题未成功，暂停并记录分析，等待人工介入
- 产出 PR 时默认附带精简的 `Summary`、`Risk`、`Validation`；只有高风险变更才附设计/迁移等展开内容
- 高风险原型阶段完成后必须暂停等待审批，不得自行进入功能实现阶段

## 完成自检（提交前必做）

每次任务完成、准备提交或汇报前，Agent 必须执行一致性自检。**优先用脚本，不依赖记忆**：

### 强制：运行 preflight 脚本（机械检查）

```bash
./scripts/preflight.sh   # 模板见 dev-rules/templates/preflight.sh
```

任何检查段失败都必须修复后重跑，**禁止带未通过的 preflight 提交**。preflight 分为默认必跑检查与高风险条件触发检查；不要把所有项目、所有任务都按最高负担执行。

### 文档数值/事实声明的硬约束

凡在 markdown/mdc 中写"X 段 / X 类 / X 条"等会随实现演进而过期的数字，**必须**用 stat 块包裹：

```markdown
verify-rules.sh 现在 <!-- stat:verify-rules-checks -->8<!-- /stat --> 段
```

新数字必须在 `dev-rules/.stats.json` 注册 compute 命令；preflight 段 8 会用 `dev-rules/sync-stats.sh --check` 自动核对。这是治"变更必伴漂移"的元解法（机制见 `digital-clone-research.md §六.¾`）。

### 人工自检（脚本无法机械化的部分）

1. **跨文件一致性**：同一概念（术语、措辞、语义）在多个文件中是否一致（脚本可检测数字 + 路径，但语义漂移需人眼）
2. **新旧对齐**：变更是否与 `docs/approved/`、`CLAUDE.md`、已有 Rules 的约定冲突
3. **冗余消除**：同一信息是否在多处用不同措辞重复（漂移源头）
4. **示例准确性**：代码示例、JSON 样例、目录结构图是否反映当前实际状态
5. **载体分工**：commit、PR body、review comment、设计文档是否各司其职，而不是重复铺陈

发现问题直接修复，修复后再提交。如果不确定是否应该改，在 PR 说明中标注待确认项。

### 升级原则（OPC 自动化）

如果某项「人工自检」反复在 review 中被发现遗漏，**必须**把它转化为机械检查：
- **数值/事实漂移** → 新增 stat 块到文档 + `.stats.json` 注册 compute 命令
- **路径/结构漂移** → 新增检查段到 `dev-rules/verify-rules.sh`（仓库内）或 `scripts/preflight.sh`（项目内）
- **契约/格式漂移** → 新增 schema 到 `dev-rules/schemas/`

把"靠自觉"转化为"靠脚本"是 OPC 哲学的硬约束，不是建议。

## 云端 Agent 调用 Claude Code CLI

当需要在云端 VM 中执行 `claude -p` 命令（审查、拆解、校准等）时：

1. 先运行 `bash scripts/setup-claude-code.sh` 检查并安装 Claude Code CLI
2. API Key 通过 Cursor Dashboard → Cloud Agents → Secrets 配置（`ANTHROPIC_API_KEY`）
3. 安装完成后即可执行 `claude -p "..." --max-budget-usd N`
