---
description: 禁止在未经用户明确授权的情况下执行破坏性 shell 命令
alwaysApply: true
---

# 破坏性命令使用规范

执行以下命令前，**必须先在对话中向用户说明意图并等待明确确认**，不得自行决定执行：

- `rm`、`rm -rf`（删除文件/目录）
- `rmdir`（删除目录）
- `git reset --hard`、`git clean -fd`（丢弃工作区变更）
- `drop`、`truncate`（数据库破坏性操作）
- `kill -9`、`pkill -f`（强制终止进程）
- `chmod 777` 或降低安全性的权限变更
- 任何向远程推送的 `force push`

## 替代方案

| 危险操作 | 安全替代 |
|---------|---------|
| `rm file` | 用 `trash file`（可恢复）；或先展示路径让用户确认 |
| `rm -rf dir/` | 先 `ls dir/` 展示内容，用户确认后再操作 |
| 批量删除 | 先 `echo` 列出将要删除的文件列表，等用户 OK |
