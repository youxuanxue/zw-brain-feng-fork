---
description: 按风险匹配 Story 与测试强度；默认先补核心测试，高风险再进入完整 Story 闭环。
alwaysApply: true
---

# 测试设计方法论

默认原则：**功能性变更必须有测试**，但不是所有变更都要先建完整 User Story 档案。

- 默认路径：先补核心测试，证明行为正确。
- 完整 Story 路径：只有命中高风险触发条件时，才进入 Story → 用例 → 对齐门禁的完整闭环（折叠在文末 §10–§13）。
- 若常规风险变更使用了 `docs/spec-delta-*.md`，其中的核心 scenario 必须至少映射到测试、手动验证步骤或明确的「未验证」说明；spec delta 不是测试替代品。

**测试聚焦（Jobs）**：先测核心，再测边界。优先级：核心正向 AC > 核心负向 AC > 回归保护 > 边界/输入空间 > 并发时序。

**测试自动化（OPC）**：所有测试必须可在 CI 中自动运行；需要前置环境的统一用 testcontainer / fixture，禁止依赖手动启动的本地服务或快照。

---

## 1. 默认路径触发与最小动作

所有功能性变更至少满足：

1. 新增或更新能验证真实行为的测试。
2. Bug 修复必须有复现 + 修复断言。
3. 无法自动化验证时，明确标注「未验证」。

以下情况**不强制**建 `.testing/user-stories/` 文件：纯文档/注释、格式化/lint、无公共 API 变化的依赖升级、无公共行为变化的内部重构（但已有测试必须原样通过）、范围清晰的小型行为修复且可被聚焦测试直接覆盖。

| 变更类型 | 最小动作 |
|---|---|
| 新增功能 | ≥1 正向 + ≥1 负向测试 |
| 行为变更 | 更新或新增对应测试 |
| Bug 修复 | 新增复现 + 修复断言测试 |
| 删除功能 | 清理对应测试代码 |

## 2. 风险升级到完整 Story 路径

**风险等级由 `rules/product-dev.mdc` 单一裁决。** 下列条目是「候选」触发器；必须**同时**命中 `product-dev.mdc` 的高风险条件（公共契约 / 不可逆状态 / 安全边界 / 高回滚成本骨架 / 核心体验高爆炸半径）才升级，不要单看条目就升级。

- 新增功能（路由/页面/CLI/后台任务）
- 行为变更（返回结构/状态码/字段语义/业务规则）
- 状态机变更（增删状态或迁移边）
- 安全/防御变更（鉴权/校验/权限模型）
- 影响用户行为的基础设施变更（缓存/Schema 迁移/重试策略）

未达高风险按默认路径补测试即可，不强制建 Story。

## 3. 验证纪律（所有路径必须）

### 运行才算验证

- 阅读代码 ≠ 验证；必须实际运行并观察输出。
- 全绿是线索不是结论，仍需触达真实行为。
- 无法运行时明确说「未验证」，不暗示成功。

### 测试失败归因

测试失败 = 代码行为与测试期望不一致 ≠ 代码有 bug。先诊断差异、再判断归属（代码 bug 还是测试过期/写错）、只修根因，禁止同时改代码和断言来对冲失败，不得为通过测试而删功能。

### Mock 选择

- 单元测试：可 mock 外部依赖。
- 集成测试：优先真实依赖；mock 隐藏行为差异时换 testcontainer。

### 验证深度与风险匹配

- 低风险：确认不报错。
- 高风险：按下方 §11 四类风险展开。
- Bug 修复：复现 → 验证修复 → 确认无影响。
- 重构：已有测试原样通过 + 公共 API 表面无变化。

### 禁止存在性测试

仅断言文件存在 / 非空 / 行数、仅检查报告已生成、用 grep 替代行为验证——**全部不能**作为验收依据。验收结论必须基于行为断言、返回结果、副作用或运行时观测。

由 `dev-rules/scripts/check_existence_only_tests.py` 机械化扫描（preflight 段位会标出违规函数）。

### 诚实报告

失败就说失败并附输出；未验证就说未验证；不粉饰，不加多余免责声明。

---

> 下面 §10–§13 仅在「完整 Story 路径」（已命中高风险）启用；低风险与常规风险跳过此节。

## 10. 故事推导（测什么）

从产品结构推导，不靠经验拍脑袋。

1. **角色 × 能力**：每个角色在每个核心能力上的动作 → 故事候选。
2. **实体生命周期**：每条状态迁移边（含失败回退边）→ 必须有故事。
3. **系统事件**：定时任务/Webhook/阈值触发/过期处理 → 必须有故事。
4. **防御需求**：每个能力的误用/滥用约束 → 必须有故事。

**覆盖门禁**：角色×能力矩阵无空格、状态迁移无漏边、系统事件无未映射故事。

## 11. 用例设计（四类风险 + 维度矩阵）

每个 InTest/Done 故事必须在 Risk Focus 中声明覆盖了哪些风险，不涉及的须注明原因。

| 风险类型 | 典型场景 |
|---|---|
| 逻辑错误 | 参数处理、鉴权判定、计费一致性、状态迁移 |
| 行为回归 | 返回结构/字段语义变化、关键链路中断、跨模块契约破坏 |
| 安全问题 | 越权访问、白名单误判、绕过防护、缺少二次校验 |
| 运行时问题 | 重启恢复、超时传播、重试幂等、并发竞争、资源耗尽 |

| 维度 | 何时展开 |
|---|---|
| 正向路径 | 始终 |
| 输入空间 | 行为变更或输入校验相关 |
| 前置状态 | 状态机、迁移、缓存、重试 |
| 副作用 | 有副作用时 |
| 并发时序 | 有写操作或幂等要求时 |
| 权限角色 | 有权限区分或安全风险时 |

按风险选择需要展开的维度；不是每个维度都默认补 case。

## 12. 故事落盘规范

- 根路径：`.testing/user-stories/`（项目目录内）；索引：`.testing/user-stories/index.md`；故事：`stories/US-<3位编号>-<kebab-case>.md`；证据：`attachments/`。
- 必填字段：ID、Title、As a / I want / So that、Trace、Risk Focus、Acceptance Criteria（含正向 + 负向 + 回归）、Assertions、Linked Tests（`file::TestFunction` + 可执行运行命令）、Evidence、Status（Draft → Ready → InTest → Done / Archived）。

```markdown
# US-XXX-slug
- ID: US-XXX
- Title: ...
- As a / I want / So that: ...
- Trace: [来源轴线]
- Risk Focus:
  - 逻辑错误：[场景]
  - 行为回归：[场景]
  - 安全问题：[场景] 或 "不适用：[原因]"

## Acceptance Criteria
1. AC-001 (正向): Given ... When ... Then ...
2. AC-002 (负向): Given ... When ... Then 返回错误并拒绝...
3. AC-003 (回归): Given 代码变更 When 执行 TestUSXXX_* Then 全部通过

## Assertions / Linked Tests / Evidence / Status
```

## 13. Story ↔ Test 对齐

- 命名：测试函数 `TestUSxxx_ScenarioName`，文件 `usXXX_slug_test.go`。
- Linked Tests 格式 `file::TestFunction`，禁止只引用实现文件，每条故事至少一条可执行运行命令。
- **漂移检测**（每次提交）：每个 AC → 有测试函数覆盖；Linked Tests 引用真实可运行；声明的 Risk Focus 必须有对应负向/边界断言。
- 项目维护 `.testing/user-stories/verify_quality.py` 时，preflight 自动跑：可执行命令 ✓ | 负向场景 ✓ | 可观测断言 ✓ | 风险类别 ✓。报告输出到 `.testing/user-stories/attachments/story-quality-report.md`。
