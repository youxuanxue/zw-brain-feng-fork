---
description: 按风险匹配 Story 与测试强度；默认先补核心测试，高风险再进入完整 Story 闭环。
alwaysApply: true
---

# 测试设计方法论

默认原则：**功能性变更必须有测试**，但不是所有变更都要先建完整 User Story 档案。

- 默认路径：先补核心测试，优先证明行为正确
- 完整 Story 路径：只有命中触发条件时，才进入 Story → 用例 → 对齐门禁的完整闭环
- 若常规风险变更使用了 `docs/spec-delta-*.md`，其中的核心 scenario 必须至少映射到测试、手动验证步骤或明确的「未验证」说明；spec delta 不是测试替代品

三层保障只在完整 Story 路径中展开：测什么不漏（故事覆盖）→ 怎么测够深（用例覆盖）→ Story 与 Test 不漂移（对齐门禁）。

**测试聚焦（乔布斯原则）**：先测核心场景，再测边界。一个故事如果连核心 AC 都没有测试，谈论 6 维矩阵无意义。优先级：核心正向 AC > 核心负向 AC > 回归保护 > 边界/输入空间 > 并发时序。

**测试自动化（OPC 原则）**：所有测试必须可在 CI 中自动运行，禁止依赖手动步骤、本地数据库快照、手动启动的外部服务。需要前置环境的，统一用 testcontainer / fixture。

---

## 1. 触发条件

### 默认路径（所有功能变更）

所有功能性变更至少满足：

1. 新增或更新能验证真实行为的测试
2. Bug 修复必须有复现 + 修复断言
3. 无法自动化验证时，明确标注「未验证」

以下情况**不强制**创建 `.testing/user-stories/` 文件：

- 纯文档/注释
- 格式化/lint
- 无公共 API 变化的依赖升级
- 无公共行为变化的内部重构（但已有测试必须原样通过）
- 范围清晰的小型行为修复，且可由聚焦测试直接覆盖

### Spec / Scenario → Test 链路

当变更存在 `docs/spec-delta-*.md`、`docs/approved/` 或 `.testing/user-stories/` 时，验证应从意图出发，而不是从实现反推：

1. spec delta 的关键 scenario → 至少有对应测试、手动验证步骤或明确「未验证」说明。
2. `docs/approved/` 的审批基线 → 高风险实现必须有测试或 evidence 覆盖核心流程与风险点。
3. 完整 Story 路径 → AC 必须追到 Linked Tests，并由 `verify_quality.py`（如项目启用）检查漂移。

不要为了补链路而生成空泛 Story；低风险和清晰小修复仍按默认最小测试动作执行。

### 进入完整 Story 路径的变更

1. 新增功能（路由/页面/CLI/后台任务）。
2. 行为变更（返回结构/状态码/字段语义/业务规则）。
3. 状态机变更（增删状态或迁移边）。
4. 安全/防御变更（鉴权/校验/权限模型）。
5. 影响用户行为的基础设施变更（缓存/Schema 迁移/重试策略）。

### 最小动作

| 变更类型 | 默认动作 | 升级动作 |
|----------|----------|----------|
| 新增功能 | ≥1 正向 + ≥1 负向测试 | 命中上方触发条件时，新建 `US-*.md` |
| 行为变更 | 更新或新增对应测试 | 命中上方触发条件时，更新 AC / Assertions / Risk Focus |
| Bug 修复 | 新增复现 + 修复断言测试 | 若修复涉及业务规则/状态机/安全，追加回归 AC |
| 删除功能 | 清理测试代码 | 若已存在 Story，则标记 Archived |

---

## 2. 故事推导（测什么，完整 Story 路径启用）

从产品结构推导，不靠经验拍脑袋。

### 4 条推导轴线

1. **角色 × 能力**：每个角色在每个核心能力上的动作 → 故事候选。
2. **实体生命周期**：每条状态迁移边（含失败回退边）→ 必须有故事。
3. **系统事件**：定时任务/Webhook/阈值触发/过期处理 → 必须有故事。
4. **防御需求**：每个能力的误用/滥用约束 → 必须有故事。

**覆盖门禁**：只在完整 Story 路径中要求角色×能力矩阵无空格、状态迁移无漏边、系统事件无未映射故事。默认路径先保证核心 AC 被测试触达。

---

## 3. 故事落盘规范（完整 Story 路径启用）

### 目录与命名

- 根路径：`.testing/user-stories/`（项目目录内，禁止写入家目录/临时目录）。
- 索引：`.testing/user-stories/index.md`（ID/标题/状态/路径）；故事状态变更必须同步更新。
- 故事：`.testing/user-stories/stories/US-<3位编号>-<kebab-case>.md`，编号全局唯一。
- 证据：`.testing/user-stories/attachments/`。

### 必填字段

| # | 字段 | 要求 |
|---|------|------|
| 1 | ID | 全局唯一 |
| 2 | Title | 故事简称 |
| 3 | As a / I want / So that | 用户叙事 |
| 4 | Trace | 来源轴线 |
| 5 | Risk Focus | 覆盖的风险类型 + 具体场景（见§4 四类风险） |
| 6 | Acceptance Criteria | Given/When/Then，必含正向 + 负向 + 回归保护 |
| 7 | Assertions | 可观测断言（状态码/字段值/副作用/错误），失败必须非 0 退出 |
| 8 | Linked Tests | `file::TestFunction` 格式引用 + 可执行运行命令 |
| 9 | Evidence | 输出归档路径 |
| 10 | Status | Draft → Ready → InTest → Done / Archived |

### 故事模板

```markdown
# US-XXX-slug

- ID: US-XXX
- Title: ...
- Version: MVP / V1.0 / V2.0
- Priority: P0 / P1 / P2
- As a / I want / So that: 作为[角色]，我希望[动作]，以便[价值]
- Trace: [来源轴线]
- Risk Focus:
  - 逻辑错误：[场景]
  - 行为回归：[场景]
  - 安全问题：[场景]（不涉及则写"不适用：[原因]"）

## Acceptance Criteria

1. AC-001 (正向): Given ...，When ...，Then ...
2. AC-002 (负向): Given ...，When ...，Then 返回错误并拒绝...
3. AC-003 (回归): Given 代码变更，When 执行 TestUSXXX_*，Then 全部通过

## Assertions

- [断言1: 预期值/行为]
- [断言N: ...]

## Linked Tests

- `path/to/test_file`::`TestUSXXX_Scenario`
- 运行命令: `go test -tags tt -v ./path -run 'TestUSXXX_'`

## Evidence

- `.testing/user-stories/attachments/[output-file]`

## Status

- [x] Done
```

---

## 4. 用例设计（怎么测）

### 四类风险覆盖（完整 Story 路径 MUST）

每个 InTest/Done 故事必须在 Risk Focus 中声明覆盖了哪些风险，不涉及的须注明原因。

| 风险类型 | 典型场景 |
|----------|----------|
| 逻辑错误 | 参数处理、鉴权判定、计费一致性、状态迁移 |
| 行为回归 | 返回结构/字段语义变化、关键链路中断、跨模块契约破坏 |
| 安全问题 | 越权访问、白名单误判、绕过防护、缺少二次校验 |
| 运行时问题 | 重启恢复、超时传播、重试幂等、并发竞争、资源耗尽 |

### 默认最小测试动作

默认先覆盖：

1. 核心正向路径
2. 核心负向路径
3. 回归保护（若是 bug 修复或行为变更）

如果这三层都还没补齐，不要先扩展矩阵。

### 按风险展开的用例矩阵

| 维度 | 核心问题 | 何时展开 |
|------|----------|----------|
| 正向路径 | 按故事描述能走通？ | 始终 |
| 输入空间 | 无效值/边界值/特殊值行为正确？ | 行为变更或输入校验相关 |
| 前置状态 | 不同起始状态下行为一致？ | 状态机、迁移、缓存、重试 |
| 副作用 | 关联数据/通知/日志/统计都对？ | 有副作用时 |
| 并发时序 | 同时操作/重复提交/中断重试？ | 有写操作或幂等要求时 |
| 权限角色 | 谁能做/谁不能/越权返回什么？ | 有权限区分或安全风险时 |

完整 Story 路径下，按风险选择需要展开的维度；不是默认每个维度都补 1 个 case。

### 禁止存在性测试

以下不能作为验收依据：仅断言文件存在/非空/行数、仅检查报告已生成、用 grep 替代行为验证。
验收结论必须基于行为断言、返回结果、副作用或运行时观测。

---

## 5. Story ↔ Test 对齐（已启用 Story 工作流时）

### 命名约定

- 测试函数：`TestUSxxx_ScenarioName`（xxx = 故事编号）。
- 测试文件：`usXXX_slug_test.go`（与故事编号对应）。
- 跨故事共享测试可被多个故事引用。

### Linked Tests 规则

- 格式 `file::TestFunction`，禁止只引用实现文件。
- 每个故事至少一条可直接执行的运行命令。

### 漂移检测（已启用 Story 工作流时提交前必检）

1. 每个 AC → 有测试函数覆盖。无覆盖则补测试或删 AC。
2. Linked Tests 引用 → 文件/函数真实存在且可运行。已删除/重命名则同步更新故事。
3. Risk Focus 声明 → 测试中有对应负向/边界场景。声明安全风险但无越权断言 = 漂移。

### 质量门禁（项目已启用时）

当项目维护了 `.testing/user-stories/verify_quality.py` 时，自动校验每个故事：
可执行命令 ✓ | 负向场景 ✓ | 可观测断言 ✓ | 风险类别 ✓

报告输出到 `.testing/user-stories/attachments/story-quality-report.md`。

---

## 6. 验证纪律

### 运行才算验证

- 阅读代码 ≠ 验证 — 必须实际运行并观察输出。
- 全绿是线索不是结论 — 仍需触达真实行为确认功能正确。
- 无法运行时明确说「未验证」，不暗示成功。

### 测试失败归因

测试失败 = 代码行为与测试期望不一致 ≠ 代码有 bug。

1. 诊断差异：代码做什么？测试期望什么？
2. 判断归属：代码 bug 还是测试过期/写错？
3. 只修根因：禁止同时改代码和断言来对冲失败。
4. 不删功能：不得为通过测试而删除已有功能。

### Mock 选择

- 单元测试：可 mock 外部依赖。
- 集成测试：优先真实依赖。Mock 隐藏行为差异时换 testcontainer。

### 验证深度与风险匹配

- 低风险：确认不报错。
- 高风险（公共契约、状态机/迁移、安全边界、高回滚成本骨架变化）：按§4 展开。
- Bug 修复：复现 → 验证修复 → 确认无影响。
- 重构：已有测试原样通过 + 公共 API 表面无变化。

### 诚实报告

失败就说失败并附输出。未验证就说未验证。不粉饰，不加多余免责声明。
