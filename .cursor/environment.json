{
  "install": "bash -lc 'cd \"${PWD:-.}\" && (test -x .venv/bin/python || python3 -m venv .venv) && .venv/bin/pip install -q -U pip && .venv/bin/pip install -q -e \".\"'",
  "start": "bash scripts/cloud-preview.sh"
}
