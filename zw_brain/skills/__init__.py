"""Skill catalog — atomic capabilities exposed to all 4 entries.

Per D2 / D3, every external-visible capability MUST be implemented as a
Skill registered under `zw_brain.capability_registry.registered`. Embedding
business logic in agents / orchestrator is forbidden.
"""
