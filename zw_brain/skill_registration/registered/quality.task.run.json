{
  "skill_id": "quality.task.run",
  "slug": "quality.task.run",
  "title": "触发自动检测任务",
  "description": "R6 一键触发某条规则的检测任务执行；任务执行在外部执行器，本端只写一次 quality_evidence_projection + audit_event，结果由外部回写。",
  "version": "1.0.0",
  "input_schema": {
    "type": "object",
    "required": ["rule_code", "confirmed"],
    "properties": {
      "tenant_id": { "type": "string" },
      "role": { "type": "string" },
      "confirmed": { "type": "boolean" },
      "rule_code": { "type": "string" },
      "target_catalog_code": { "type": "string" },
      "scope_payload_json": { "type": "object" }
    }
  },
  "output_schema": { "type": "object" },
  "side_effects": ["audit", "db_write"],
  "tenant_scope": "tenant",
  "auth_policy": "user",
  "human_confirmation_required": true,
  "audit_class": "write-default",
  "execution_binding": "builtin",
  "compatibility": ["webui", "api", "cli", "mcp", "a2a"],
  "runtime_binding": {
    "kind": "brain_service",
    "surface_entry": "zw_brain.command.brain.BrainService.invoke_skill",
    "method": "quality.task.run"
  },
  "rollback_target": null,
  "audit_required": true,
  "permissions": ["quality.task.run.execute"],
  "author": { "org": "inspur-cloud", "contact": "zw-brain@inspur.com" },
  "registry_source": "internal",
  "tags": ["quality", "task", "r6", "write"]
}
