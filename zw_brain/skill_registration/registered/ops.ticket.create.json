{
  "skill_id": "ops.ticket.create",
  "slug": "ops.ticket.create",
  "title": "创建运维工单",
  "description": "安全审计员 创建运维工单（告警处理 / 巡检 / 拨测 / 安全 / 其他），指派给责任人。",
  "version": "1.0.0",
  "input_schema": {
    "type": "object",
    "properties": {
      "ticket_type": { "type": "string", "enum": ["alert", "inspection", "dial-test", "security", "other"] },
      "title": { "type": "string" },
      "assignee": { "type": "string" },
      "description": { "type": "string" },
      "role": { "type": "string" },
      "confirmed": { "type": "boolean" }
    },
    "required": ["confirmed", "title"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "result": { "type": "object" },
      "ok": { "type": "boolean" },
      "summary": { "type": "object" }
    }
  },
  "side_effects": ["audit", "db_write"],
  "tenant_scope": "tenant",
  "auth_policy": "user",
  "human_confirmation_required": true,
  "audit_class": "write-normal",
  "execution_binding": "builtin",
  "compatibility": ["webui", "api", "cli", "a2a"],
  "runtime_binding": {
    "kind": "brain_service",
    "surface_entry": "zw_brain.command.brain.BrainService.invoke_skill",
    "method": "ops.ticket.create"
  },
  "rollback_target": null,
  "audit_required": true,
  "permissions": ["ops.ticket.create.execute"],
  "author": {
    "org": "inspur-cloud",
    "contact": "zw-brain@inspur.com"
  },
  "registry_source": "internal",
  "tags": ["ops", "ticket", "write"]
}
