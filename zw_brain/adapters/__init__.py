"""One-way sync adapters from legacy systems → zw-brain Agent-native models (D7).

The brain never reaches into legacy schemas; adapters pull + transform into
the new model. Reverse direction is forbidden.
"""
