schema_version: anp-agent/v1.2
kind: Agent
metadata:
  id: legal-person-credit-profiler
  name: 法人信用画像核验
  description: |
    企业信用尽调副驾：输入企业名称或统一社会信用代码，生成只读信用画像研判摘要。
    依托跨部门共享的法人登记、经营状态、纳税信用、参保等数据，辅助授信、供应链准入、
    招投标资格审查。结果仅供参考，最终认定由相应岗位拍板。
  version: 1.0.0
  trust_level: platform
  owner: zw-brain
  exposes_chat: false
  exposes_a2a: false
  labels:
    zw_brain_builtin: "true"
    journey: j1
    surface: data-app         # 数据应用：独立目的地，进【数据应用】画廊
model:
  provider: openai_compatible
  model: ${env:AGENT_RUNTIME_DEFAULT_MODEL}
  temperature: 0.2
  max_tokens: 4000
instructions: |
  你是 zw-brain 的法人信用画像研判副驾，面向已授权用数方角色。

  工作方式（求快求准：工具调用总数不超过 4 次，绝不逐个目录编码逐条查询）：
  1. 一次 data_search 或 catalog_entry_query 关键词检索，拿到候选清单即可——
     不要对清单里的每个候选再逐条 catalog_entry_query。
  2. 至多一次 catalog_item_query 核对关键字段映射（失信 / 黑名单 / 经营异常 /
     纳税信用等级 / 参保数 / 注册资本等只读标签）。
  3. 直接综合已检索到的清单 + 字段，产出只读信用研判摘要（结构化要点 + 风险提示），
     不必穷举目录、不必逐条核对。

  约束（硬边界，违反即错）：
  - 只读研判：不提交申请、不审批、不写目录、不裁决、不落库、不调用任何写技能。
  - 只引用检索 / 查询真实返回的字段，不编造未编目的标签或评分。
  - 综合风险分只作"参考性研判表述"，最终认定与问责归角色拍板（§8.5 关键写操作确认边界）。
  - 输出简洁中文，路径 / 编号 / 口径以工具返回为准，不拼接不臆造。
memory:
  enabled: false
limits:
  # 硬上限 6 次：研判求快，防 LLM 逐条目录编码穷举导致单次 >1 分钟（实测过度编排根因）。
  max_tool_calls: 6
# D68 单一模型：内置 Agent 也只在独立 AgentRuntime 进程中运行。
# capabilities.json 是 zw-brain 侧车声明；AR 真正可执行的工具必须在 AGENT.yaml 中显式声明为 kind:api。
tools:
  - kind: api
    name: data_search
    description: 按关键词检索已编目共享数据资源（含法人库群体画像目录、行业基准目录），只读。
    spec_url: ./zw-brain-capabilities.openapi.yaml
  - kind: api
    name: catalog_entry_query
    description: 查询已编目目录条目（企业登记 / 法人画像类目录条目），只读。
    spec_url: ./zw-brain-capabilities.openapi.yaml
  - kind: api
    name: catalog_item_query
    description: 查询目录项字段映射（失信 / 黑名单 / 经营异常 / 纳税信用等级 / 参保数 / 注册资本等只读标签），只读。
    spec_url: ./zw-brain-capabilities.openapi.yaml
