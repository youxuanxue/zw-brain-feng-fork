scope: wave0-agentruntime-embedded
signed_by: 薛娇（产品研发负责人）
date: 2026-06-03
kind: 决策签字
evidence: >
  2026-06-03 工程决策签收（基础设施工程 scope，非业务走查——AgentRuntime Embedded SDK 无业务页）。
  wave0-infra（2026-05-31）当时枚举 6 个 infra feature 但**遗漏** infra-agentruntime-embedded，
  本条独立补签收口该遗漏。依据：

  1) **回测真绿**：tests/test_agentruntime_embedded.py 5 用例在 py312 + vendored AgentRuntime SDK 环境
     全绿（validate 内置 Agent AGENT.yaml / doctor readiness gate / 负向 spec_version 拒绝 / zw-brain
     Capability provider 进程内调用 / Embedded SDK fake-core 异步任务）。测量产物
     .testing/status/measurement/ffcfb8ba…json captured_at=2026-06-03T14:44:09Z 记 result=pass、
     指纹新鲜（D46.g 内容指纹门）。SDK 是 vendor/agent-runtime py312-pyc-only 离线包（CI .[dev] 不装→
     importorskip，故此 feature 测量须在 py312+SDK 环境采，见 capture python 覆盖 ZW_BRAIN_CAPTURE_PYTHON）。

  2) **机械守卫持续护航**：preflight 段 51 agentruntime_validate.py / check_agentruntime_bundles.py 对
     agents/zw_search_helper + agents/zw_platform_guide 两份 AGENT.yaml + capabilities.json（anp-agent/v1.2）
     持续校验（不依赖 SDK runtime，纯结构守卫）。

  3) **落地溯源**：Embedded SDK 最小集成 PR #152/#155 落地两份内置 Agent bundle。

  **本期 scope 严格限 Embedded SDK 进程内最小集成**；外部 Agent 接入 pilot（ext-agent-pilot）与
  Standalone HTTP 形态（agentruntime-standalone-http）仍按 D30 T1 / 基线 §8.2 触发式延后，已各自标
  # Deferred → Backlog，不在本签收范围。
decision_only: false
covers:
  - .testing/waves/wave-0-golden-path/features/infra-agentruntime-embedded.feature
