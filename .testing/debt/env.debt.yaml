slug: env
title: "验收证据 CI 化采集（消除人工采集 env 依赖）"
date: 2026-05-29
severity: medium
owner: 推理平台（集团）
trigger: "见 docs/preflight-debt.md 历史归档"
assert:
  # 纯立项/业务决策门延后，无 code 条件可断言（external 须 owner+trigger，trigger 触发即升级）。
  kind: external
