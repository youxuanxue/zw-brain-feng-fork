slug: j1-credential-revoke
title: "j1-credential-revoke WebUI 撤回入口未铺（本地走查 #8，选 B 留 InTest）"
date: 2026-05-31
severity: medium
owner: 产品研发负责人
trigger: "铺好 P4/P3 撤回 UI（接 application.grant.revoke/suspend + 确认弹窗 + 申请人侧红色通知）→ 本地走查通过 → 往 .testing/signoff/ 追加 covers j1-credential-revoke → 现算自动翻 Done。属 webui-capability-render-debt（docs/webui-capability-render-debt.md）一类"
assert:
  # 现算（D46.g）：撤回能力仍在 webui 渲染豁免台账（= 声称 WebUI 供应但无渲染消费者）→ open；
  # 铺好撤回 UI 后该 slug 从台账移除 → absent → stale-fixed。锚台账（段52）比 grep vue 树稳。
  kind: grep_present
  file: scripts/webui_capability_rendered_exemptions.txt
  pattern: "application\\.grant\\.revoke"
