slug: j1-runtime-write-path-dual-track
title: "运行时申请写路径仍走演示时代双轨（内存快照 dict escape hatch）→ Action D 写路径单源化待立项"
date: 2026-06-11
severity: medium
owner: 产品研发负责人
# permission-matrix-0610（PR #245）走查实锤；负责人 2026-06-11 裁「B 先 A 后」。
#
# 【方案 B 已兑现 · fix/j1-conditional-accept-bridge】状态词汇桥接 + 二级可办修复：
#   1. 有条件 (shared_type=2) 直提/草稿提交单落 status='submitted' 进 D55/P21 受理两级队列
#      （解析链：payload 显式值 → resource.accessPolicy → resource_asset access_policy 回源）；
#      无条件保持 'pending' 单步受理即终。
#   2. 运行时单随单存档 owner_org_code（R11 方向 guard 对空 owner fail-closed——此前所有
#      运行时/legacy 单 owner 均空 → 二级部门审核对任何管理员 403、单据永卡 dept_approved，
#      旧 e2e「审核通过」断言被按钮自身文案假绿遮蔽）。
#   3. 条件状态机迁移回写内存快照行（_sync_snapshot_request_status）——否则凭据自动签发
#      hook 读陈旧态把已成功的终审响应炸成 409、管理员工作台待办不投影。
#   4. dev-bypass 会话 org 可配置（ZW_BRAIN_DEV_IAM_BYPASS_ORG，start-local 默认省大数据局），
#      方向 guard 语义不变、dev 走查可走通二级。
#   验证：tests/test_j1_conditional_status_bridge.py（5 用例）+ permission_matrix_walkthrough
#   业务流 1 全链自铸真 UI（不再依赖预铸单）。
#
# 【方案 A 残留 · 本债继续盯】演示时代写路径本体未消灭：request.create /
# application.resource.submit 仍先写 BrainService 内存快照 dict（铸 REQ-NNN），再经
# PersistMiddleware → sync_reference_tables 镜像 upsert 进 application_record；读写双轨由
# 桥接与回写勉力同步（_sync_snapshot_request_status 即 Action D 落地后应退役的过渡件）。
trigger: "Action D 立项（运行时写路径单源化：request.create/submit 直写 application_record、消灭内存快照 dict 与 REQ-* 编号序列、退役 _sync_snapshot_request_status 过渡件与 sync_reference_tables 申请镜像）。属架构门，方案须负责人审批后实施。"
assert:
  # 机械锚点：创建路径仍写内存快照 dict（Action C escape hatch 注释行）。Action D 落地
  # 删除该写法后本断言转 stale-fixed 提醒收账。
  kind: grep_present
  file: zw_brain/command/handlers/j1/request.py
  pattern: '_snapshot\["requests"\]\.insert\(0, request\)'
