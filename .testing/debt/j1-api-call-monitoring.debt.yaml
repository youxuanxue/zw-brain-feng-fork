slug: j1-api-call-monitoring
title: "j1-api-call-monitoring 无 P4 调用监控 UI（本地走查，留 InTest）"
date: 2026-05-31
severity: medium
owner: 产品研发负责人
trigger: "铺 P4 调用监控 UI（接 capability_call 查询 + 配额/QPS 展示）→ 走查 → 往 .testing/signoff/ 追加 covers → 翻 Done。属 webui-capability-render-debt 一类"
assert:
  # 评估（D46.g）：拟转 grep 但 capability_call 非 live+webui 能力、不在渲染豁免台账，
  # 无干净单文件锚点；硬转 grep vue 树会制造假信号（注释命中即误判已铺）。故保持 external，
  # 由 trigger（铺 P4 监控 UI → 追加 signoff covers）人工升级。
  kind: external
