slug: credential-grant-invariant
title: "凭据签发不变量未机械化（credential ⟺ accessGrantSnapshot）"
date: 2026-05-31
severity: medium
owner: 产品研发负责人
trigger: "授权调用型交付（带 accessGrantSnapshot）漏签 credential、或回流/汇总型交付误挂 credential 被发现——届时落 scripts/check_credential_grant_invariant.py 守卫并补单测"
assert:
  # 现状（2026-05-31 代码核实）：DLV 交付的 credential 规则靠数据约定、无代码强制不变量。
  #   - 授权调用型交付（如 DLV-0003 出生一件事跨域同步）带 accessGrantSnapshot.credential；
  #   - 回流/汇总型交付（如 DLV-0011 停车场预填→回流）整个 accessGrantSnapshot 键缺席、本就不签发凭据。
  # 数据自洽但非机械守卫 → 改 seed/导入态时无防回归。
  # open ⇔ 守卫脚本尚不存在；创建 scripts/check_credential_grant_invariant.py（断言
  # "有 accessGrantSnapshot 才允许/要求 credential"）→ pattern 出现 → 自动 stale-fixed。
  kind: grep_absent
  file: scripts/preflight.sh
  pattern: "check_credential_grant_invariant"
